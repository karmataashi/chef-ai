export default function Hero() {
  return (
    <main>
      <form className="ingredient-form">
        <input type="text" placeholder="eg: onions" aria-label="ingredient" />
        <button>Add ingredients</button>
      </form>
    </main>
  );
}
